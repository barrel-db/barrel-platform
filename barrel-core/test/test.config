[
  {barrel, [
    {wpool_opts, [{overrun_warning, 100}]},
    {default_store, barrel_test_rocksdb},
    
    {stores, [
      {barrel_test_rocksdb, barrel_rocksdb_store, [
        {workers, 71},
        {dir, "testdb"}
      ]}
    ]},

    {http_server, [{port, 8080}]}
  ]},

  {sasl, [
    {sasl_error_logger, false}
  ]}
].
